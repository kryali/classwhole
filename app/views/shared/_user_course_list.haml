%h1 Your Courses 
- unless current_user_has_courses?
  %span.hint Don't have any classes yet, add some?
%ul
  - if current_user
    - for course in current_user.courses
      %li
        = link_to("X", remove_course_path(course.id), :remote => true, :class=>"remove-link")
        %span= course.to_s
= render 'shared/class_search'

= javascript_include_tag "autocomplete"
= javascript_include_tag "user.course.list"
= javascript_include_tag "forms/schedule-autocomplete"
