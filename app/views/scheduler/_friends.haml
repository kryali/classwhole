%ul.friends
  %h2 Friends
  - user.friends.each do |friend|
    %li
      = link_to "", scheduler_show_path(friend.fb_id), :style => "background:url(\"#{fb_img_link( friend, :square)}\") no-repeat;", :title => friend.name
