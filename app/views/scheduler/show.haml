= javascript_include_tag "scheduler.js"
%div.scheduler-content
  %div#slides
    %div.slides_container
      - @possible_schedules.each do |sections|
        = render "schedule", :sections => sections
    %a{:href=>"#", :class=>"prev"}
    %a{:href=>"#", :class=>"next"}
    -if !@possible_schedules.empty?
      %div.mini-pagination-top
        -#%a{:href=>"#", :class=>"mini-prev"} <
        -#%a{:href=>"#", :class=>"mini-next"} >
        %div.mini-pagination-container
          %ul.mini-pagination
            - i = 1
            - @possible_schedules.each do |sections|
              - if i == 1
                %li.current
                  %a{ :href => "##{i-1}" }
                  = render 'mini_schedule', :sections => sections
              -else 
                %li
                  %a{ :href => "##{i-1}" }
                  = render 'mini_schedule', :sections => sections
              - i += 1

  %div{ :id => "register-modal", :class => "modal hidden" }
    %div.modal-header
      %a{ :class => "close-modal", :style => "cursor:pointer" }×
      %h3 Register
    %div.modal-body
      %p#crns Example CRNS:
      %iframe{ :src => "https://apps.uillinois.edu/selfservice/", :width => "100%", :height => "350px" }
    %div.modal-footer
      %a{:class=>"btn primary close-modal"} Close

:javascript
  var course_ids = #{@course_ids};
